#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        for (int i = 0; i < n; i++)
        {
            long long x, y;
            cin >> x >> y;
        }
        long long X, Y;
        cin >> X >> Y;
        cout << "! " << X << " " << Y << "\n";
        cout.flush();
    }
}
